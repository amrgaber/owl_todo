<templates>
    <t t-name="ProductCategorySidebar">
        <div class="col-2 o_category_list text-center mt-4 ms-1">
            <t t-foreach="props.categories" t-as="category" t-key="category.id">
                <span t-esc="category.name"></span>
            </t>
        </div>
        <!-- <div class="auction_category active" data-id="all" t-on-click="onClickCategory">
                <span class="category_selected" t-translation="off">All</span>
            </div>
            <div t-foreach="props.categorProductCategorySidebaries" t-key="category.id">
                <span t-esc="category.name"></span>
            </div> -->
    </t>
</templates>